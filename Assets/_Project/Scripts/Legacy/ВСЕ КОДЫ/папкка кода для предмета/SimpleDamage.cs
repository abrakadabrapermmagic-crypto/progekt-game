using UnityEngine;

public class SimpleDamage : MonoBehaviour
{

    [Header("Settings")]
    public int damage = 10;
    public float attackCooldown = 1.5f;
    public float attackRange = 2f;
    [SerializeField] private float hitRangeTolerance = 0.5f;
    [SerializeField] private bool applyDamageImmediately = true;
    // Метод срабатывает каждый раз, когда кто-то входит в триггер
      }
